@model IEnumerable<SI2Proy.Models.UnidadMedida>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model=>model.CodUnidadMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NombreUnidadMedida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Abreviatura)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem=> item.CodUnidadMedida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NombreUnidadMedida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Abreviatura)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodUnidadMedida">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodUnidadMedida">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodUnidadMedida">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
